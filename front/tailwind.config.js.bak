export default {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: '#D97706', // 琥珀金
        accent: '#FDE68A',  // 奶油黄
        bgWarm: '#FFFBF0',  // 暖米白
        textDark: '#431407', // 深褐
        textMuted: '#92400E', // 琥珀褐
      },
      borderRadius: {
        'custom': '16px',
      },
      boxShadow: {
        'warm': '0 20px 50px -12px rgba(217, 119, 6, 0.15)',
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.5s ease-out forwards',
        'pop': 'pop 0.3s cubic-bezier(0.26, 0.53, 0.74, 1.48) forwards',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        pop: {
          '0%': { transform: 'scale(0.9)' },
          '100%': { transform: 'scale(1)' },
        }
      }
    },
  },
  plugins: [],
}
