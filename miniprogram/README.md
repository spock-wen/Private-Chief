# 🍲 家庭厨房点菜小程序

## 项目简介

家庭厨房点菜小程序是一款专为家庭聚餐、朋友聚会设计的轻量级点菜协作工具。通过微信小程序实现主人创建饭桌、客人在线点菜的协作流程，有效解决线下口头点菜易遗漏、难统计的问题。

## 功能特色

- **饭桌管理**：创建、分享、状态维护
- **菜单管理**：增删改查、状态设置
- **多角色协作**：主人端+客人端
- **实时点菜**：与数据同步
- **数据导出**：文本/图片

## 技术架构

- 前端：微信小程序原生框架
- 后端：微信云开发（Node.js云函数）
- 数据库：微信云数据库

## 资源优化说明

> **重要提示**：项目中的图片资源需要进行优化以满足微信小程序的大小要求。
> 
> 特别是tabBar图标（home.png, home-active.png, history.png, history-active.png）必须小于40KB。
> 
> 请参考 [images/OPTIMIZATION_GUIDE.md](images/OPTIMIZATION_GUIDE.md) 文件进行图片资源优化。
```

d:\Devlopment\LiteProgram\Private chef\README.md
```markdown
<<<<<<< SEARCH
## 📦 项目结构

```
.
├── app.js                 # 小程序入口文件
├── app.json              # 小程序配置文件
├── app.wxss              # 全局样式
├── pages/                # 页面目录
│   ├── index/           # 首页
│   ├── host/            # 主人端页面
│   │   ├── create-table/    # 创建饭桌
│   │   ├── menu-manage/     # 菜单管理
│   │   ├── view-orders/     # 查看点菜
│   │   ├── confirm-menu/    # 确认菜单
│   │   └── history/         # 历史记录
│   └── guest/           # 客人端页面
│       ├── enter-table/     # 进入饭桌
│       ├── menu-view/       # 查看菜单
│       └── order-dish/      # 点菜管理
├── utils/               # 工具函数
│   ├── api.js          # API调用
│   └── util.js         # 通用工具
├── cloudfunctions/     # 云函数
│   ├── createTable/    # 创建饭桌
│   ├── addDish/        # 添加菜品
│   ├── orderDish/      # 点菜
│   ├── confirmMenu/    # 确认菜单
│   ├── deleteDish/     # 删除菜品
│   ├── updateDish/     # 更新菜品
│   ├── deleteOrder/    # 删除订单
│   └── updateOrder/    # 更新订单
└── images/             # 图片资源

```

## 🗄️ 数据库结构

### tables 集合（饭桌）
```javascript
{
  _id: String,              // 饭桌ID
  name: String,             // 饭桌名称
  diningDate: String,       // 用餐日期
  diningTime: String,       // 用餐时间
  remark: String,           // 备注
  collectPreferences: Boolean, // 是否收集口味偏好
  needPhoneAuth: Boolean,   // 是否需要手机号验证
  status: String,           // 状态: active, confirmed, finished
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

### menus 集合（菜单）
```javascript
{
  _id: String,              // 菜品ID
  tableId: String,          // 所属饭桌ID
  name: String,             // 菜品名称
  category: String,         // 分类（热菜、凉菜、汤品等）
  price: Number,            // 价格
  description: String,      // 描述
  status: String,           // 状态: available, recommended, soldout
  image: String,            // 图片URL
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

### orders 集合（订单/点菜记录）
```javascript
{
  _id: String,              // 订单ID
  tableId: String,          // 所属饭桌ID
  userId: String,           // 用户ID
  userName: String,         // 用户昵称
  dishId: String,           // 菜品ID
  dishName: String,         // 菜品名称
  quantity: Number,         // 数量
  note: String,             // 备注
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

## 🚀 快速开始

### 1. 准备工作

1. 在微信公众平台注册小程序账号
2. 开通云开发服务
3. 获取 AppID 和云开发环境ID

### 2. 配置项目

1. 修改 `app.js` 中的云开发环境ID：
```javascript
wx.cloud.init({
  env: 'your-env-id', // 替换为你的云开发环境ID
  traceUser: true,
});
```

2. 修改 `project.config.json` 中的 AppID：
```json
{
  "appid": "your-appid"
}
```

### 3. 创建数据库集合

在云开发控制台中创建以下集合：
- `tables`（饭桌）
- `menus`（菜单）
- `orders`（订单）

### 4. 部署云函数

1. 在微信开发者工具中，右键点击 `cloudfunctions` 目录
2. 选择"上传并部署：云端安装依赖"
3. 为每个云函数目录重复此操作

### 5. 配置数据库权限

在云开发控制台的数据库中，为每个集合设置以下权限：
- 读取：所有用户可读
- 写入：仅创建者可写（或根据需求调整）

### 6. 运行项目

1. 使用微信开发者工具打开项目
2. 点击"编译"按钮
3. 在模拟器或真机上测试

## 📝 使用说明

### 主人端使用流程

1. **创建饭桌**
   - 点击"创建饭桌"
   - 输入饭桌名称、选择用餐时间
   - 填写备注信息
   - 选择是否需要收集口味偏好

2. **管理菜单**
   - 添加菜品（名称、分类、价格、描述）
   - 设置菜品状态（可点、推荐、售罄）
   - 编辑或删除菜品

3. **分享饭桌**
   - 点击"分享饭桌"
   - 生成二维码或复制链接
   - 分享给客人

4. **查看点菜情况**
   - 查看所有客人的点菜
   - 可按人或按菜品查看

5. **确认菜单**
   - 确认最终菜单
   - 导出菜单文本或图片

### 客人端使用流程

1. **进入饭桌**
   - 扫描二维码或输入链接
   - 输入昵称

2. **点菜**
   - 浏览菜单
   - 点击"+"增加数量
   - 点击"-"减少数量
   - 可添加备注

3. **查看点菜**
   - 查看我的点菜
   - 查看所有人的点菜

## 🎨 界面预览

- 主色调：#ff6b35（橙红色）
- 卡片式设计
- 响应式布局

## 🔧 开发说明

### API调用说明

所有API调用都在 `utils/api.js` 中封装，使用云函数或云数据库API。

### 实时同步

当前版本使用轮询方式更新数据，后续可以升级为WebSocket实时同步。

### 图片上传

菜品图片上传功能需要配置云存储，当前版本使用默认占位图。

## 📄 License

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

如有问题，请提交 Issue。
## 📦 项目结构

详见 [项目结构说明](STRUCTURE.md)

## 🚀 快速开始

详见 [快速开始指南](QUICKSTART.md)

## 🛠️ 部署说明

详见 [部署指南](SETUP.md)

## ▶️ 运行指南

详见 [运行指南](运行指南.md)

## 📝 使用说明

详见 [使用说明文档](USAGE.md)

## 🎨 界面预览

详见 [界面设计文档](DESIGN.md)

## 🔧 开发说明

详见 [开发文档](DEVELOPMENT.md)

## 📄 License

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

如有问题，请提交 Issue。

## 📚 更多文档

- [CHANGELOG.md](CHANGELOG.md) - 开发日志
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - 常见问题

## 📦 项目结构

```
.
├── app.js                 # 小程序入口文件
├── app.json              # 小程序配置文件
├── app.wxss              # 全局样式
├── pages/                # 页面目录
│   ├── index/           # 首页
│   ├── host/            # 主人端页面
│   │   ├── create-table/    # 创建饭桌
│   │   ├── menu-manage/     # 菜单管理
│   │   ├── view-orders/     # 查看点菜
│   │   ├── confirm-menu/    # 确认菜单
│   │   └── history/         # 历史记录
│   └── guest/           # 客人端页面
│       ├── enter-table/     # 进入饭桌
│       ├── menu-view/       # 查看菜单
│       └── order-dish/      # 点菜管理
├── utils/               # 工具函数
│   ├── api.js          # API调用
│   └── util.js         # 通用工具
├── cloudfunctions/     # 云函数
│   ├── createTable/    # 创建饭桌
│   ├── addDish/        # 添加菜品
│   ├── orderDish/      # 点菜
│   ├── confirmMenu/    # 确认菜单
│   ├── deleteDish/     # 删除菜品
│   ├── updateDish/     # 更新菜品
│   ├── deleteOrder/    # 删除订单
│   └── updateOrder/    # 更新订单
└── images/             # 图片资源

```

## 🗄️ 数据库结构

### tables 集合（饭桌）
```javascript
{
  _id: String,              // 饭桌ID
  name: String,             // 饭桌名称
  diningDate: String,       // 用餐日期
  diningTime: String,       // 用餐时间
  remark: String,           // 备注
  collectPreferences: Boolean, // 是否收集口味偏好
  needPhoneAuth: Boolean,   // 是否需要手机号验证
  status: String,           // 状态: active, confirmed, finished
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

### menus 集合（菜单）
```javascript
{
  _id: String,              // 菜品ID
  tableId: String,          // 所属饭桌ID
  name: String,             // 菜品名称
  category: String,         // 分类（热菜、凉菜、汤品等）
  price: Number,            // 价格
  description: String,      // 描述
  status: String,           // 状态: available, recommended, soldout
  image: String,            // 图片URL
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

### orders 集合（订单/点菜记录）
```javascript
{
  _id: String,              // 订单ID
  tableId: String,          // 所属饭桌ID
  userId: String,           // 用户ID
  userName: String,         // 用户昵称
  dishId: String,           // 菜品ID
  dishName: String,         // 菜品名称
  quantity: Number,         // 数量
  note: String,             // 备注
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

## 🚀 快速开始

### 1. 准备工作

1. 在微信公众平台注册小程序账号
2. 开通云开发服务
3. 获取 AppID 和云开发环境ID

### 2. 配置项目

1. 修改 `app.js` 中的云开发环境ID：
```javascript
wx.cloud.init({
  env: 'your-env-id', // 替换为你的云开发环境ID
  traceUser: true,
});
```

2. 修改 `project.config.json` 中的 AppID：
```json
{
  "appid": "your-appid"
}
```

### 3. 创建数据库集合

在云开发控制台中创建以下集合：
- `tables`（饭桌）
- `menus`（菜单）
- `orders`（订单）

### 4. 部署云函数

1. 在微信开发者工具中，右键点击 `cloudfunctions` 目录
2. 选择"上传并部署：云端安装依赖"
3. 为每个云函数目录重复此操作

### 5. 配置数据库权限

在云开发控制台的数据库中，为每个集合设置以下权限：
- 读取：所有用户可读
- 写入：仅创建者可写（或根据需求调整）

### 6. 运行项目

1. 使用微信开发者工具打开项目
2. 点击"编译"按钮
3. 在模拟器或真机上测试

## 📝 使用说明

### 主人端使用流程

1. **创建饭桌**
   - 点击"创建饭桌"
   - 输入饭桌名称、选择用餐时间
   - 填写备注信息
   - 选择是否需要收集口味偏好

2. **管理菜单**
   - 添加菜品（名称、分类、价格、描述）
   - 设置菜品状态（可点、推荐、售罄）
   - 编辑或删除菜品

3. **分享饭桌**
   - 点击"分享饭桌"
   - 生成二维码或复制链接
   - 分享给客人

4. **查看点菜情况**
   - 查看所有客人的点菜
   - 可按人或按菜品查看

5. **确认菜单**
   - 确认最终菜单
   - 导出菜单文本或图片

### 客人端使用流程

1. **进入饭桌**
   - 扫描二维码或输入链接
   - 输入昵称

2. **点菜**
   - 浏览菜单
   - 点击"+"增加数量
   - 点击"-"减少数量
   - 可添加备注

3. **查看点菜**
   - 查看我的点菜
   - 查看所有人的点菜

## 🎨 界面预览

- 主色调：#ff6b35（橙红色）
- 卡片式设计
- 响应式布局

## 🔧 开发说明

### API调用说明

所有API调用都在 `utils/api.js` 中封装，使用云函数或云数据库API。

### 实时同步

当前版本使用轮询方式更新数据，后续可以升级为WebSocket实时同步。

### 图片上传

菜品图片上传功能需要配置云存储，当前版本使用默认占位图。

## 📄 License

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

如有问题，请提交 Issue。


